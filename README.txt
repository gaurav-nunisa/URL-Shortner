send email and _id throught the body only works in the localhost:8000 postman
cannot work in the browser beacuse you can set the bearer header and the email and _id in the body / header of h
the browser 